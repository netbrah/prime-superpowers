#!/usr/bin/env bash
set -euo pipefail

script_dir=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)
export PATH="$script_dir/toolchain/node_modules/.bin:$PATH"
exec node "$script_dir/lib/launcher.mjs" "$@"
